const products=[{id:1,name:'Chicken Burger',price:450,icon:'🍔',desc:'Juicy chicken burger with fresh salad.'},{id:2,name:'Chicken Biryani',price:350,icon:'🍛',desc:'Aromatic rice with tender chicken.'},{id:3,name:'Pizza',price:900,icon:'🍕',desc:'Cheesy pizza prepared fresh.'},{id:4,name:'Chicken Roll',price:250,icon:'🌯',desc:'Crispy roll with seasoned chicken.'},{id:5,name:'French Fries',price:180,icon:'🍟',desc:'Golden crispy potato fries.'},{id:6,name:'Cold Drink',price:120,icon:'🥤',desc:'Refreshing chilled drink.'}];let cart=JSON.parse(localStorage.getItem('easyFoodCart')||'[]');const menuGrid=document.getElementById('menuGrid'),cartList=document.getElementById('cartList'),cartCount=document.getElementById('cartCount'),cartTotal=document.getElementById('cartTotal'),cartEl=document.getElementById('cart'),backdrop=document.getElementById('backdrop');const money=n=>`Rs ${n.toLocaleString()}`;
function renderMenu(){menuGrid.innerHTML=products.map(p=>`<article class="food"><div class="food-image">${p.icon}</div><div class="food-body"><h3>${p.name}</h3><p>${p.desc}</p><div class="food-row"><span class="price">${money(p.price)}</span><button class="add" onclick="addItem(${p.id})">Add to cart</button></div></div></article>`).join('')}
function addItem(id){const item=cart.find(x=>x.id===id);item?item.qty++:cart.push({id,qty:1});save();openCart()}
function changeQty(id,amount){const item=cart.find(x=>x.id===id);if(!item)return;item.qty+=amount;if(item.qty<1)cart=cart.filter(x=>x.id!==id);save()}
function save(){localStorage.setItem('easyFoodCart',JSON.stringify(cart));renderCart()}
function renderCart(){let total=0,count=0;if(!cart.length){cartList.innerHTML='<div class="empty">Your cart is empty.<br>Add something delicious from the menu.</div>'}else{cartList.innerHTML=cart.map(x=>{const p=products.find(y=>y.id===x.id),line=p.price*x.qty;total+=line;count+=x.qty;return `<div class="cart-item"><div><strong>${p.name}</strong><small>${money(p.price)} × ${x.qty}</small></div><div class="qty"><button onclick="changeQty(${p.id},-1)">−</button><button onclick="changeQty(${p.id},1)">+</button></div></div>`}).join('')}cartCount.textContent=count;cartTotal.textContent=money(total)}
function openCart(){cartEl.classList.add('open');backdrop.classList.add('show')}function closeCart(){cartEl.classList.remove('open');backdrop.classList.remove('show')}
document.getElementById('cartButton').onclick=openCart;document.getElementById('contactButton').onclick=openCart;document.getElementById('closeCart').onclick=closeCart;backdrop.onclick=closeCart;document.getElementById('mobileMenu').onclick=()=>document.getElementById('nav').classList.toggle('open');document.getElementById('checkout').onclick=()=>{if(!cart.length){alert('Please add an item first.');return}alert('Your order has been prepared. Delivery and payment confirmation will be connected in the next step.')};document.getElementById('year').textContent=new Date().getFullYear();renderMenu();renderCart();