import './globals.css';

export const metadata = {
  title: 'Easy Food — Food Delivery in Pakistan',
  description: 'A simple, transparent food ordering and delivery marketplace.',
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
